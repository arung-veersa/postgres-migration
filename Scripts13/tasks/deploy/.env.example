# Conflict Management Pipeline - ECS Task Definition Environment Variables
# =========================================================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# The deploy script (build-and-push-ecr.ps1) reads this file to resolve
# placeholders in ecs-task-definition.json before registering with ECS.
#
# IMPORTANT: .env is gitignored -- never commit secrets to source control.
# =========================================================================

# --- Snowflake (read-only source) ---
SNOWFLAKE_ACCOUNT=your-account.us-east-1
SNOWFLAKE_USER=CONFLICTREPORT_USER
SNOWFLAKE_WAREHOUSE=CONFLICTREPORT_WH
SNOWFLAKE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBg...\n-----END PRIVATE KEY-----

# --- PostgreSQL (read/write target) ---
POSTGRES_HOST=your-cluster.us-east-1.rds.amazonaws.com
POSTGRES_USER=conflict_admin
POSTGRES_PASSWORD=your-password-here

# --- AWS / Email ---
AWS_REGION=us-east-1
EMAIL_SENDER=noreply@example.com
EMAIL_RECIPIENTS=user1@example.com,user2@example.com
